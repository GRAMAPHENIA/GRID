---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Portfolio from '../../components/sections/Portfolio.astro';
import { getInstruments } from '../../data/instruments';

// Obtener todos los instrumentos
const instruments = getInstruments();

// Datos SEO para la página de trabajos
const seo = {
  title: 'Portfolio de Instrumentos | Red Fang Luthier',
  description: 'Explora la colección completa de instrumentos musicales artesanales creados por Red Fang Luthier. Guitarras, bajos, violines y mandolinas construidos con técnicas tradicionales y maderas seleccionadas.',
  keywords: ['luthier', 'instrumentos musicales', 'guitarras artesanales', 'portfolio', 'Red Fang', 'construcción instrumentos'],
  canonical: '/works',
  structuredData: {
    "@context": "https://schema.org",
    "@type": "CollectionPage",
    "name": "Portfolio de Instrumentos - Red Fang Luthier",
    "description": "Colección de instrumentos musicales artesanales creados por Red Fang Luthier",
    "url": "https://redfangluthier.com/works",
    "mainEntity": {
      "@type": "ItemList",
      "numberOfItems": instruments.length,
      "itemListElement": instruments.map((instrument, index) => ({
        "@type": "Product",
        "position": index + 1,
        "name": instrument.name,
        "description": instrument.description,
        "category": instrument.type,
        "offers": {
          "@type": "Offer",
          "price": instrument.price,
          "priceCurrency": "EUR",
          "availability": instrument.status === 'available' 
            ? "https://schema.org/InStock" 
            : "https://schema.org/OutOfStock"
        }
      }))
    }
  }
};
---

<BaseLayout seo={seo}>
  <main class="min-h-screen bg-pure-white">
    <!-- Hero section para la página de trabajos -->
    <section class="hero-section pt-20 md:pt-24 pb-8 md:pb-12 px-4">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold text-almost-black mb-4 md:mb-6">
          Portfolio de <span class="text-neutral-700">Instrumentos</span>
        </h1>
        <p class="text-lg md:text-xl text-neutral-600 mb-8 md:mb-12 max-w-3xl mx-auto leading-relaxed">
          Cada instrumento es una obra de arte única, creada con pasión y dedicación utilizando técnicas tradicionales de lutería y maderas cuidadosamente seleccionadas.
        </p>
        
        <!-- Estadísticas rápidas -->
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-8 mt-8 md:mt-16">
          <div class="text-center">
            <div class="text-2xl md:text-3xl font-bold text-almost-black mb-1 md:mb-2">
              {instruments.length}
            </div>
            <div class="text-neutral-500 text-xs md:text-sm font-medium">
              Instrumentos
            </div>
          </div>
          <div class="text-center">
            <div class="text-2xl md:text-3xl font-bold text-green-600 mb-1 md:mb-2">
              {instruments.filter(i => i.status === 'available').length}
            </div>
            <div class="text-neutral-500 text-xs md:text-sm font-medium">
              Disponibles
            </div>
          </div>
          <div class="text-center">
            <div class="text-2xl md:text-3xl font-bold text-almost-black mb-1 md:mb-2">
              {instruments.filter(i => i.featured).length}
            </div>
            <div class="text-neutral-500 text-xs md:text-sm font-medium">
              Destacados
            </div>
          </div>
          <div class="text-center">
            <div class="text-2xl md:text-3xl font-bold text-almost-black mb-1 md:mb-2">
              {new Set(instruments.map(i => i.type)).size}
            </div>
            <div class="text-neutral-500 text-xs md:text-sm font-medium">
              Tipos
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Componente Portfolio con filtros -->
    <Portfolio 
      instruments={instruments} 
      showFilters={true}
      title="Colección Completa"
      description="Filtra por tipo de instrumento para encontrar exactamente lo que buscas"
    />

    <!-- Sección de llamada a la acción -->
    <section class="py-12 md:py-16 px-4 bg-neutral-50">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-2xl md:text-3xl lg:text-4xl font-bold text-almost-black mb-4 md:mb-6">
          ¿No encuentras lo que buscas?
        </h2>
        <p class="text-lg md:text-xl text-neutral-600 mb-6 md:mb-8 leading-relaxed">
          Cada músico es único, y su instrumento también debería serlo. Hablemos sobre tu proyecto personalizado.
        </p>
        <div class="flex flex-col sm:flex-row gap-3 md:gap-4 justify-center">
          <a 
            href="/contact"
            class="px-6 md:px-8 py-3 bg-almost-black text-pure-white rounded-md font-medium hover:bg-neutral-800 transition-colors duration-200 text-sm md:text-base"
          >
            Solicitar Instrumento Personalizado
          </a>
          <a 
            href="/services"
            class="px-6 md:px-8 py-3 border border-almost-black text-almost-black rounded-md font-medium hover:bg-almost-black hover:text-pure-white transition-colors duration-200 text-sm md:text-base"
          >
            Ver Servicios
          </a>
        </div>
      </div>
    </section>
  </main>
</BaseLayout>